part of '../earbuds_compare_page.dart';

class _PaTab extends StatelessWidget {
  const _PaTab();

  @override
  Widget build(BuildContext context) {
    return const _MetricTableView(group: MetricGroup.pa);
  }
}
