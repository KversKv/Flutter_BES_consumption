import 'package:flutter/material.dart';
import 'earbuds_compare_page.dart'; // ✅ 新增导入

class WifiPage extends StatelessWidget {
  const WifiPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const EarbudsComparePage();
  }
}